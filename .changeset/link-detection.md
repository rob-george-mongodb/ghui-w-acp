---
"@kitlangton/ghui": patch
---

Detect and open links in PR bodies. Markdown `[label](url)` and bare URLs are highlighted, hover changes the cursor to a pointer, and clicking opens the link in the system browser. Single-pass tokenizer also handles `#NNN` references and inline code. New `link` color in every theme.
